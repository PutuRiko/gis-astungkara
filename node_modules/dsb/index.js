module.exports = (function(DEBUG){
/*---------------------------------------------------------------------*/    
  
  //setup like imports are stored above the breaker
  var release = require('./release.txt');

/***********************************************************************/

  /*
   * @method dsb
   * this is the format all "meat" methods follow
   * @param name {type} description
   * @param [optional] {type} optional params similar to YUIDoc
   * @param [setting=true] {object} declare a default value using [{name}={default}]
   * @note primitive types can be lowercase, 
   *   but you should Upper_snake non-native constructor objects
   * @todo add dsb-utils, or at least a few modules.
   * @return always {any} document if something is returned
   */

/*---------------------------------------------------------------------*/
  
  var dsb = function(){
    
    var r = release;
    
    if( DEBUG ) console.log(r);
    
    return r;
    
  };

/***********************************************************************/

  return dsb;
  
/*---------------------------------------------------------------------*/
}(0));